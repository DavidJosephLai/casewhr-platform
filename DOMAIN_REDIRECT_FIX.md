# 🔧 網域重定向修復指南

## ❌ 問題：casewhere.com.tw 顯示空白頁

### 📋 可能的原因：

1. **Vercel 配置缺少重定向規則** ✓ 已修復
2. **DNS 還沒完全生效**
3. **Vercel 網域設定不完整**

---

## ✅ 已完成的修復

### 1. 更新 `vercel.json`
已添加 `redirects` 規則：
```json
{
  "redirects": [
    {
      "source": "/:path*",
      "has": [
        {
          "type": "host",
          "value": "casewhere.com.tw"
        }
      ],
      "destination": "https://casewhr.com/:path*",
      "permanent": true,
      "statusCode": 301
    }
  ]
}
```

這會自動將 `casewhere.com.tw` 重定向到 `casewhr.com`。

---

## 🚀 立即執行的步驟

### **步驟 1：推送更新到 GitHub**
```bash
git add vercel.json
git commit -m "Fix: Add domain redirect for casewhere.com.tw"
git push origin main
```

### **步驟 2：在 Vercel 檢查網域設定**

1. 前往：https://vercel.com/dashboard
2. 選擇您的專案
3. 點擊 **Settings** → **Domains**
4. 確認以下設定：

#### ✅ 應該看到的配置：
```
主網域（Production）：
✓ casewhr.com

額外網域（Redirect）：
✓ casewhere.com.tw → Redirects to casewhr.com
✓ www.casewhere.com.tw → Redirects to casewhr.com
```

---

## 🔍 診斷步驟

### **測試 1：檢查 DNS 是否生效**
```bash
# Windows
nslookup casewhere.com.tw

# 應該顯示 Vercel 的 IP
# 76.76.21.21 或類似的 IP
```

### **測試 2：檢查 Vercel 部署狀態**
1. 前往 Vercel Dashboard
2. 查看最新部署是否成功
3. 確認 `vercel.json` 更新已被採用

### **測試 3：清除瀏覽器緩存**
- **Windows**: `Ctrl + Shift + Delete`
- **Mac**: `Cmd + Shift + Delete`
- 選擇「清除全部」

### **測試 4：使用無痕模式**
```
Chrome: Ctrl + Shift + N
Edge: Ctrl + Shift + P
```
訪問：https://casewhere.com.tw

---

## 📱 Vercel 網域設定指南

### **如何在 Vercel 添加網域：**

#### **步驟 1：進入專案設定**
```
Vercel Dashboard → Your Project → Settings → Domains
```

#### **步驟 2：添加網域**
點擊 **"Add Domain"** 按鈕

#### **步驟 3：輸入網域**
```
casewhere.com.tw
```

#### **步驟 4：選擇重定向選項**
選擇：
```
☑ Redirect to casewhr.com
```

#### **步驟 5：配置 DNS**
Vercel 會顯示需要設定的 DNS 記錄：
```
Type: A
Name: @
Value: 76.76.21.21

Type: CNAME
Name: www
Value: cname.vercel-dns.com
```

---

## 🌐 DNS 設定檢查

### **在網域註冊商設定 DNS：**

#### **選項 A：A 記錄（推薦）**
```
類型：A
主機名：@
指向：76.76.21.21
TTL：3600
```

#### **選項 B：CNAME 記錄**
```
類型：CNAME
主機名：@
指向：cname.vercel-dns.com
TTL：3600
```

#### **WWW 子網域**
```
類型：CNAME
主機名：www
指向：cname.vercel-dns.com
TTL：3600
```

---

## ⏰ 生效時間

- **最快**：15 分鐘
- **通常**：1-2 小時
- **最慢**：24-48 小時

### **如何檢查是否生效：**
```bash
# 檢查 A 記錄
nslookup casewhere.com.tw

# 檢查 CNAME
nslookup www.casewhere.com.tw

# 線上工具
https://dnschecker.org/#A/casewhere.com.tw
```

---

## 🔧 故障排除

### **問題 1：還是空白頁**
**可能原因：**
- DNS 還沒生效（等待 1-2 小時）
- Vercel 沒有識別到網域

**解決方案：**
1. 清除瀏覽器緩存
2. 使用無痕模式測試
3. 檢查 Vercel Domains 設定
4. 重新部署：`vercel --prod`

### **問題 2：顯示 "404 - Not Found"**
**可能原因：**
- Vercel 配置錯誤

**解決方案：**
1. 檢查 `vercel.json` 是否正確推送
2. 在 Vercel Dashboard 查看部署日誌
3. 確認網域已添加到專案

### **問題 3：重定向循環**
**可能原因：**
- 重定向規則配置錯誤

**解決方案：**
1. 檢查 `vercel.json` 的 redirects 配置
2. 確保只重定向 `casewhere.com.tw`，不重定向 `casewhr.com`

---

## 🎯 驗證重定向是否成功

### **方法 1：瀏覽器開發者工具**
1. 打開 `casewhere.com.tw`
2. 按 `F12` 打開開發者工具
3. 切換到 **Network** 標籤
4. 重新載入頁面
5. 查看第一個請求的狀態碼

**應該看到：**
```
Status: 301 Moved Permanently
Location: https://casewhr.com/
```

### **方法 2：線上工具**
```
https://httpstatus.io/
輸入：https://casewhere.com.tw
```

**應該顯示：**
```
301 → https://casewhr.com
```

### **方法 3：命令行**
```bash
curl -I https://casewhere.com.tw
```

**應該看到：**
```
HTTP/2 301
location: https://casewhr.com/
```

---

## 📊 完整檢查清單

### ✅ Vercel 設定
- [ ] 網域已添加到 Vercel 專案
- [ ] 選擇「Redirect to casewhr.com」
- [ ] `vercel.json` 已更新並推送
- [ ] 最新部署成功

### ✅ DNS 設定
- [ ] A 記錄指向 76.76.21.21
- [ ] CNAME 記錄指向 cname.vercel-dns.com
- [ ] TTL 設定為 3600 或更低
- [ ] 等待 DNS 生效（1-2 小時）

### ✅ 測試
- [ ] 清除瀏覽器緩存
- [ ] 使用無痕模式測試
- [ ] 檢查開發者工具的 Network 標籤
- [ ] 驗證 301 重定向
- [ ] 確認最終到達 casewhr.com

---

## 🆘 如果還是不行

### **快速診斷命令：**
```bash
# 檢查 DNS
nslookup casewhere.com.tw

# 檢查 HTTP 狀態
curl -I https://casewhere.com.tw

# 檢查完整重定向鏈
curl -L -I https://casewhere.com.tw
```

### **聯絡支援：**
如果以上都無法解決，可能需要：
1. 檢查網域註冊商的 DNS 設定
2. 聯絡 Vercel 支援
3. 確認網域沒有被其他服務佔用

---

## 📝 預期結果

**成功後的行為：**
```
用戶訪問：https://casewhere.com.tw
↓
瀏覽器收到：301 Moved Permanently
↓
重定向到：https://casewhr.com
↓
顯示：完整的 CaseWHR 網站
```

**用戶體驗：**
- 網址列顯示：`casewhr.com`（不是 casewhere.com.tw）
- 頁面正常顯示
- 所有功能正常運作

---

## 🎉 成功標誌

當以下條件都滿足時，設定成功：

1. ✅ 訪問 `casewhere.com.tw` 自動跳轉到 `casewhr.com`
2. ✅ 網址列顯示 `casewhr.com`
3. ✅ 頁面完整顯示（不是空白頁）
4. ✅ 開發者工具顯示 `301` 狀態碼
5. ✅ 所有子頁面都正常工作（如 `/pricing`）

---

**最後更新：** 2026-01-06
**狀態：** 配置已修復，等待 DNS 生效
